# Laravel Boilerplate (PHP-FPM + Nginx Unix Socket + PostgreSQL)

Этот репозиторий представляет собой **boilerplate** для быстрого развертывания Laravel-проектов с оптимизированной архитектурой взаимодействия сервисов.

## 🚀 Особенности

*   **Производительность:** Связь между Nginx и PHP-FPM настроена через **Unix Socket**, что быстрее классического TCP соединения.
*   **Современный стек:** PHP 8.4 (Alpine), Nginx, PostgreSQL 17.
*   **Удобство разработки:** Готовый интерфейс **pgAdmin 4** для работы с БД.
*   **Автоматизация:** Полный набор команд в `Makefile` для управления контейнерами и Laravel.
*   **Xdebug:** Предустановлен и готов к включению одной переменной в `.env`.

## 📂 Структура проекта

*   `docker/` — Docker-файлы и конфигурации для PHP и Nginx.
*   `docker-compose.yml` — Описание инфраструктуры.
*   `Makefile` — Команды для быстрой работы (setup, up, down, artisan, etc.).
*   `SETUP.md` — Подробная инструкция по интеграции boilerplate в ваш Laravel проект.

## 🛠 Быстрый старт

Этот проект предназначен для копирования в свежесозданный Laravel-проект.

1.  Создайте проект Laravel: `composer create-project laravel/laravel .`
2.  Скопируйте файлы boilerplate (`docker/`, `docker-compose.yml`, `Makefile`) в корень.
3.  Настройте `.env` (см. детали в [SETUP.md](SETUP.md)).
4.  Запустите установку:
    ```bash
    make setup
    ```

После завершения:
*   Сайт: [http://localhost](http://localhost)
*   pgAdmin: [http://localhost:8080](http://localhost:8080)

---
*Подробная инструкция по установке находится в файле [SETUP.md](SETUP.md).*
